#set("title") {Post Index}

#set("body") {
<div>
  #for(post in posts) {
    <a href="/posts/#(post.slug)">
      <h2>#(post.title)</h2>
    </a>
    <h4>#(post.date)</h4>
  }
</div>
}

#embed("layouts/master")
